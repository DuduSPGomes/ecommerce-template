describe('Testing ProductRepository', () => {

  //const repo = new ProductRepository()


  test.only('should return a list of products from api', async () => {
    //const products = await repo.getProducts()
    //expect(products).not.toEqual(mock)
  })


})
